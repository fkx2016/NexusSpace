# ============================================================================
# NexusSpace Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your required values

# ----------------------------------------------------------------------------
# REQUIRED: OpenRouter API Configuration
# ----------------------------------------------------------------------------
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ----------------------------------------------------------------------------
# OPTIONAL: LLM Model Configuration
# ----------------------------------------------------------------------------
# Comma-separated list of council member models
COUNCIL_MODELS=google/gemini-2.0-flash-exp,meta-llama/llama-3.3-70b-instruct,anthropic/claude-3.5-haiku,mistralai/mixtral-8x7b-instruct,google/gemma-2-9b-it

# Model used for final synthesis

# Comma-separated list of allowed CORS origins
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ----------------------------------------------------------------------------
# OPTIONAL: Storage Configuration
# ----------------------------------------------------------------------------
# Storage backend: 'filesystem' or 'database' (This will be important in Task 12)
# STORAGE_BACKEND=filesystem

# Directory for file-based storage (only used if STORAGE_BACKEND=filesystem)
# DATA_DIR=data/conversations

# Directory for temporary cloned repositories
# TEMP_CLONE_DIR=temp_clones

# ----------------------------------------------------------------------------
# OPTIONAL: File Reader Configuration
# ----------------------------------------------------------------------------
MAX_FILES_TO_READ=1000
MAX_CODEBASE_SIZE_MB=50

# Comma-separated list of supported file extensions
# SUPPORTED_EXTENSIONS=.py,.js,.ts,.jsx,.tsx,.java,.go,.rs,.cpp,.c,.h,.cs,.rb,.php,.swift,.kt,.md,.txt,.json,.yaml,.yml,.toml,.xml,.html,.css,.scss,.sql,.sh,.bash,.env.example

# ----------------------------------------------------------------------------
# OPTIONAL: API Timeout Configuration
# ----------------------------------------------------------------------------
# API_TIMEOUT_SECONDS=120.0
# TITLE_GENERATION_TIMEOUT=30.0

# ----------------------------------------------------------------------------
# OPTIONAL: LLM Generation Configuration
# ----------------------------------------------------------------------------
# Maximum number of tokens to generate in response
LLM_MAX_OUTPUT_TOKENS=4096

# ----------------------------------------------------------------------------
# OPTIONAL: Debug Configuration
# ----------------------------------------------------------------------------
# Set to 1 to print configuration summary on startup
# DEBUG_CONFIG=0
